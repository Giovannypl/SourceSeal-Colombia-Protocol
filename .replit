cat > .replit << 'EOF'
language = "nodejs"
run = "cd source-seal-completo && npm start"

[env]
PORT = "3000"
NODE_ENV = "production"

[deployment]
run = ["cd", "source-seal-completo", "&&", "npm", "start"]
EOF