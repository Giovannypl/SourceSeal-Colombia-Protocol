// 路径：/server/package.json
{
  "name": "source-seal-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "NODE_ENV=development tsx index.ts",
    "build": "tsc", // 只编译TypeScript
    "start": "NODE_ENV=production node dist/index.cjs"
  },
  "dependencies": {
    // 这里只放后端需要的依赖
    "express": "^4.18.0",
    // ... 其他后端依赖
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.20.5"
    // ... 其他后端开发依赖
  }
}