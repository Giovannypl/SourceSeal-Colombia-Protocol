import express from 'express';
const app = express();
const PORT = process.env.PORT || 3000;

app.get('/', (req, res) => {
  res.json({
    status: '✅ SourceSeal v2.0 Desplegado',
    shard: 'COL-01',
    mensaje: 'Infraestructura multi-shard (Colombia & EU) operativa',
    siguiente: 'Integrando ZKP para soberanía algorítmica'
  });
});

app.get('/health', (req, res) => {
  res.status(200).send('OK');
});

app.listen(PORT, () => {
  console.log(`🚀 Servidor SourceSeal ejecutándose en el puerto ${PORT}`);
});